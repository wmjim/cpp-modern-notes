# 环境变量

## $@ 和 #^

- `$@`：表示目标文件。
- **用途**：在命令部分，`$@` 会被替换为当前规则的目标文件名。
- `$^`：所有依赖文件
- **用途**：在命令部分，`$^` 会被替换为当前规则中列出的所有依赖文件，无论依赖文件的数量有多少。

```make
program: main.o utils.o
	gcc -o $@ $^
```

`$@` 会被替换为 `program`，`$^` 会被替换为 `main.o`、`utils.o` ，最终执行的命令 `gcc -o program main.o utils.o` 。