# 纯虚函数与抽象类

```cpp
// 抽象类：只要包含纯虚函数的类，不能直接实例化
class Animal {
public:
	// 纯虚函数：没有函数体，表明接口行为必须由子类实现
	virtual void speak() = 0; // 纯虚函数
};
```

- 抽象类不能被实例化，但可作为表示通用概念的基类（如：形状、动物）。
- 抽象类的子类必须重写父类中的纯虚函数，否则也属于抽象类。
