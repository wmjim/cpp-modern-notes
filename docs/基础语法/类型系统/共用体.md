# 共用体

`union` 能够存储不同的数据类型，但只能同时存储其中的一种类型。

```cpp
// 1. 共用体声明
union Data {
    int i;
    float f;
    char c;
};

// 2. 使用共用体
Data d;
d.i = 42;
d.f = 3.14; // d.i 失效
```

- **共用体的大小**等于其**最大成员的大小**；
- 所有成员从**同一地址开始存储**，因此写入一个会覆盖另一个。